# Cloudflare Pages configuration.
# KV/R2 bindings are set in the Pages dashboard (Settings → Bindings), not here,
# so the build never depends on a placeholder id.
name = "web-trak"
pages_build_output_dir = "."
compatibility_date = "2024-11-01"
